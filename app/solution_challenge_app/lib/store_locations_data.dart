import 'package:flutter/material.dart';

class _saveLocationData extends StatelessWidget {
  const _saveLocationData({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Welcome to The Contribution Screen!'),
      ),
      body: Container(
        alignment: Alignment.center,
        child: const Text('Hi Mom!'),
      ),
    );
  }
}
