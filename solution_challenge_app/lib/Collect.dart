import 'package:flutter/material.dart';

class Collect extends StatelessWidget {
  const Collect ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Trash Items Collection Page'),
      ),
    );
  }
}
